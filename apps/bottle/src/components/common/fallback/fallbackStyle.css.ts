import { style } from '@vanilla-extract/css';

export const fallbackContainerStyle = style({
  width: '100%',
  height: 'auto',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
});
